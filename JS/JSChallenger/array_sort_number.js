// Write a function that takes an array of numbers as an argument
// It should return an array with the numbers sorted in descending order

function sortString(numbers) {
    return numbers.sort();
}

console.log(sortString([1,3,2]));